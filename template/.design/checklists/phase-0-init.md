# Phase 0 — Init · done criteria

Verified by `/design:init` (and re-checkable with `/design:check`).

- [ ] `.design/memory/toolbox.md` filled in: every row has a status (`active` / `fallback`), none left `pending`
- [ ] Git repository initialized; remote configured **or** the local-only fallback explicitly recorded in `toolbox.md`
- [ ] Hosting decided: GitHub Pages enabled **or** fallback (local static server) recorded in `toolbox.md`
- [ ] `pipeline.html` renders in a browser: phase 1 `in-progress`, phases 2–9 `locked`, all artifacts missing
- [ ] `CLAUDE.md` and `README.md` present from the template, untouched context blocks in place
- [ ] First commit made; push only if a remote is active in `toolbox.md`
